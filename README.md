# Fuel EU Maritime — Compliance Module

This repository implements a small Fuel EU Maritime compliance platform (frontend + backend) following a hexagonal architecture.

Overview:
- Frontend: React + TypeScript + Tailwind
- Backend: Node + TypeScript + Prisma + Express

See `/frontend/README.md` and `/backend/README.md` for per-service run instructions.
